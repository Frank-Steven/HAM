# 0x07 HAM 运算符及其重载

HAM 的运算符是语法糖，本质是二元函数。

```ham
a + b
```

```ham
`+(a, b)
```

运算符函数可以被覆写：

```ham
`+ = (a, b) => a * b
```

这样就把加号变成了乘法。

但我们更多时候需要扩展算符的功能：

```ham
`+ = `+ <| (a:MySet, b:MySet) => ...
```

用 Δ 算符搭配带有定义域的函数轻松扩展符号的功能，并不破坏原有功能。
