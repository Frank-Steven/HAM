# 0x07 HAM 运算符及其重载

HAM 的运算符是语法糖，本质是二元函数，以下两种写法等价：

```HAM
a + b
```

```HAM
`+(a, b)
```

运算符函数可以被覆写：

```HAM
`+ = (a, b) => a * b
```

这样就把加号变成了乘法。

但我们更多时候需要扩展算符的功能：

```HAM
`+ = `+ <| (a:MySet, b:MySet) => ...
```

用 Δ 算符搭配带有定义域的函数轻松扩展符号的功能，并不破坏原有功能。

### 运算符一览表

